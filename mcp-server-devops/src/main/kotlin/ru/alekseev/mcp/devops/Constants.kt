package ru.alekseev.mcp.devops

/**
 * API endpoint paths for MCP HTTP server
 */
object ApiEndpoints {
    const val HEALTH = "/health"
    const val MCP = "/mcp"
}

/**
 * Default configuration values
 */
object ServerDefaults {
    const val DEFAULT_PORT = 8082
}
